language: generic
sudo: required
services:
  - docker

before_install:
  - cd frontend
  - docker build -t frontent/docker_k8s -f Dockerfile .

script:
  - docker run -e CI=true frontent/docker_k8s npm run test -- --coverage
